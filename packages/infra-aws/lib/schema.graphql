input EntryInput {
    name: String!
    message: String!
}
type Entry {
    userId: String!
    name: String!
    message: String!
}

type Query {
    getEntry(input: EntryInput): Entry!
}

type Mutation {
    updateEntry(userId: ID!, lastUpdateAt: Float!, input: EntryInput!): Entry!
}

